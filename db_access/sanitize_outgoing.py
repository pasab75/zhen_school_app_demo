# -------------------------------------------------------------
# Put functions here that format outgoing data to the client
# We can do away with all the separate business objects
# Not really too much use for them as the db returns dictionaries
# We can work with the dictionaries directly and trim them before
# they get send to the client
# -------------------------------------------------------------